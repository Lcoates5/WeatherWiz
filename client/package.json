{
  "name": "client",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npx tsc && vite build",
    "preview": "vite preview",
    "render-build": "npm install && npm run build"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "dayjs": "^1.11.10"
  }
}
